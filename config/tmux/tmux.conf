# Scott Stanfield
# https://hamvocke.com/blog/a-guide-to-customizing-your-tmux-conf/

set-option -g default-terminal 'tmux-256color'
set-option -g terminal-overrides ",*:RGB"

# prefix: Use ctrl-o instead of ctrl-b 
unbind-key C-b               # pass ctrl-o down to other apps
set        -g  prefix C-o    # use ctrl-o as our <leader>
# bind       C-o send-prefix

set -g base-index 1               # start indexing windows at 1 instead of 0
set -g detach-on-destroy off      # don't exit from tmux when closing a session
set -g escape-time 0              # zero-out escape time delay
set -g history-limit 1000000      # increase history size (from 2,000)
set -g renumber-windows on        # renumber all windows when any window is closed
set -g set-clipboard on           # use system clipboard
set -g default-command "${SHELL}" # tmux spawns a login shell by default resulting in negative side effects
set -g mode-keys vi              # prefix + [   (then vim nav, space highlight, yank, enter)
set -g display-time 4000          # status messages visible for 4s
set -g status-interval 5          # refresh status bar every 5s
set -g mouse on


set -g pane-active-border-style 'fg=magenta,bg=default'
set -g pane-border-style 'fg=brightblack,bg=default'

#####################
# session / window management
#####################
bind ^X lock-server
bind ^C new-window -c "$HOME"
bind ^D detach
bind *  list-clients
bind S  choose-session
bind :  command-prompt

#####################
# pane navigation (vim-style)
#####################
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# quick pane cycling (ctrl-o twice quickly)
bind ^O select-pane -t :.+

#####################
# pane splitting
#####################
unbind %
bind v split-window -h -c "#{pane_current_path}"
bind s split-window -v -c "#{pane_current_path}"

#####################
# pane resizing
#####################
# z==zoom

bind z resize-pane -Z
bind H resize-pane -L 10
bind J resize-pane -D 10
bind K resize-pane -U 10
bind L resize-pane -R 10

#####################
# pane utilities
#####################
bind x swap-pane -D

#####################
# misc keybindings
#####################
unbind r
bind r source-file ~/.config/tmux/tmux.conf \; display "Reloaded tmux.conf"
bind , command-prompt "rename-window %%"

# Fix clear screen / yank
bind C-l send-keys 'C-l'
bind C-k send-keys 'C-k'

# copy like vim (trigger with ctrl-[
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi V send-keys -X select-line
bind -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel
bind -T copy-mode-vi Y send-keys -X copy-pipe-no-clear  # copy without exiting

# Mouse drag: copy the selection but KEEP it highlighted on release.
# tmux's default MouseDragEnd1Pane runs copy-selection-and-cancel, which
# exits copy mode and clears the highlight the moment you let go.
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-selection-no-clear
bind -T copy-mode    MouseDragEnd1Pane send-keys -X copy-selection-no-clear

#####################
# plugins
#####################
# colors (dracula-ish)
bg=#282a36
fg=#f8f8f2
purple=#bd93f9
green=#50fa7b
cyan=#8be9fd
orange=#ffb86c

set -g status-style "bg=$bg,fg=$fg"
set -g status-left-length 40
set -g status-right-length 80

# left: icon + session name
set -g status-left "#[bg=$purple,fg=$bg,bold] #{?HOST_ICON,#{HOST_ICON},☉} #S #[bg=$bg,fg=$purple]"

# right: ssh host (when set) + time
set -g status-right "#[fg=$cyan]#{?SSH_CONNECTION,#H ,}#[fg=$green] %a %b %d %H:%M:%S"

# window list
set -g window-status-current-style "bg=$orange,fg=$bg,bold"
set -g window-status-format " #I:#W "
set -g window-status-current-format " #I:#W * "


# pane borders (you already have these but here for completeness)
set -g pane-active-border-style "fg=$purple"
set -g pane-border-style "fg=brightblack"

# set -g @plugin 'dracula/tmux'
set -g @dracula-show-left-icon "#{?HOST_ICON,#{HOST_ICON},☉}"
set -g @dracula-left-icon-padding 2
set -g @dracula-show-powerline false
set -g @dracula-show-timezone false
set -g @dracula-show-border-contrast true
set -g @dracula-plugins "ssh-session time"
run-shell '~/.local/share/tmux/plugins/dracula/dracula.tmux'
